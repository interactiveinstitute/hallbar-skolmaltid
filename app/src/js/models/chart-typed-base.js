export default function (chartInit) {
  const name = '';
  const attachmentTypes = [];
  const endpoints = (attached) => {};
  const chartJSData = (values) => {};

  return {
    name: name,
    attachmentTypes: attachmentTypes,
    endpoints: endpoints,
    chartJSData: chartJSData
  };
}
